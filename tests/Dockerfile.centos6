ARG BASE_IMAGE=tlog-base-centos6
FROM ${BASE_IMAGE}

# Compression tools for multi-format rotation tests (gzip in base image; no zstd on CentOS 6)
RUN yum install -y xz bzip2 && yum clean all

COPY files/ /opt/files/
COPY tests/ /opt/tests/

WORKDIR /opt/tests
CMD ["bats", "--formatter", "tap", "/opt/tests/"]
